reporter {
  reports {
    customTemplate {
      pluginId = "PdfTemplate"
      description = "Custom template report."
    }
    webApp {
      pluginId = "WebApp"
      description = "Web app report."
    }
  }
}
